import React from 'react';
import Login from "../../components/loginComponent/Login";

const LoginPage = () => {
    return (
        <div>
            <Login/>
        </div>
    );
};

export default LoginPage;